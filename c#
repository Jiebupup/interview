1.公共语言运行时CLR
相当于java虚拟机

2.对象
属性property
方法method
事件event：一种消息机制，一个类中的一个方法调用另一个方法，另一个类中+=或-=注册或移除。需要声明委托。

3.类型
值类型：简单类型、结构类型和枚举类型，分配到栈中
引用类型：类类型、接口类型、委托类型和数组类型，分配到堆中

等价类型相当于java包装类型
推断类型var，Nullable类型？，动态类型dynamic
struct结构类型：在定义时不能给初始值，只有通过带参数的构造方法赋值。不能继承。

一个类中：字段+方法
属性相当于对字段的访问set和get
索引相当于访问字段的下标

this用于构造方法调用另一个构造方法与java不同：pubilc Person():this(0,""){}

所有事物都是对象，包括基本类型。

局部变量必须手动赋值，保存在栈中。

4.继承
class 子类 ： 父类{}
子类对父类方法的重载，new增加和虚方法的覆盖

访问控制符：internel相同程序集的非子类可见

const和readonly
sealed类表示不可继承，abstract类和方法

虚方法实现多态：virtual和override。

5.委托delegate
函数指针，参数是方法，把方法包装成变量
委托的合并：多播

lambda表达式简化版的匿名方法：=>
linq：from、where、select

允许操作符重载
编译预处理
指针

attribute类似注解：[]

6.集合
HashTable
IEnumerator代替了Iterable遍历
Dictionary字典代替Map

7.线程
join（）：两个线程合并成一个线程
并行任务库：TPL











